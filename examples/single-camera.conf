# One camera, full screen.
# Uses the MAIN stream because it gets the whole display.

CAMERAS=(
    "rtsp://admin:password@192.168.1.10:554/Streaming/Channels/101"
)
CAMERA_NAMES=( "Front Door" )

GRID_COLS=1
GRID_ROWS=1
WINDOW_GAP=0
ROTATION_INTERVAL=0
